#include <bits/stdc++.h>

using namespace std;

#define ll long long

int main() {
    int n, m; cin >> n >> m;
    string str[n];
    for(int i = 0; i < n; i++) {
        cin >> str[i];
    }

    int validOp[3] = {0};

    for(auto s : str) {
        
    }
}