#include <bits/stdc++.h>

using namespace std;

#define ll long long

int main() {
    ll tc = 1; //cin >> tc;
    for(ll t = 1; t <= tc; t++) {
        ll n, t; cin >> n >> t;
        while(t-- > 0) {
            ll k; cin >> k;
        }
    }
}